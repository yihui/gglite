% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coordinate.R
\name{coord_}
\alias{coord_}
\title{Set the Coordinate System}
\usage{
coord_(chart = NULL, type, ...)
}
\arguments{
\item{chart}{A \code{g2} object.}

\item{type}{Coordinate type string.}

\item{...}{Additional options such as \code{innerRadius}, \code{outerRadius},
\code{startAngle}, \code{endAngle}, or \code{transform}.}
}
\value{
The modified \code{g2} object.
}
\description{
Specify the coordinate system for the chart. G2 supports these coordinate
types: \code{'cartesian'} (default), \code{'polar'}, \code{'theta'}, \code{'radial'}, \code{'radar'},
\code{'helix'}, \code{'parallel'}. Use the \code{transform} argument to apply coordinate
transforms such as \code{'transpose'} (equivalent to ggplot2's \code{coord_flip()}) or
\code{'fisheye'}.
}
\details{
The \code{'parallel'} coordinate requires a \code{position} encoding (a character
vector of column names) instead of separate \code{x}/\code{y} encodings. For radar
charts, use the \code{'polar'} coordinate with long-format data (x/y/color).
}
\examples{
# Polar coordinate (rose chart)
df = data.frame(x = c('A', 'B', 'C'), y = c(3, 7, 2))
g2(df, x = 'x', y = 'y') |>
  mark_interval() |>
  coord_polar()

# Theta coordinate (pie / donut chart)
g2(df, x = 'x', y = 'y', color = 'x') |>
  mark_interval() |>
  transform_('stackY') |>
  coord_theta()

# Radial coordinate (radial bar chart)
g2(df, x = 'x', y = 'y', color = 'x') |>
  mark_interval() |>
  coord_radial()

# Parallel coordinate (uses position encoding)
g2(iris, position = names(iris)[-5], color = 'Species',
  padding = c(30, NA, NA, NA)) |>
  mark_line() |>
  coord_parallel() |>
  legend_color(position = 'bottom')

# Radar coordinate (polar with long-format data)
df2 = data.frame(
  item = rep(c('Design', 'Dev', 'Marketing', 'Sales', 'Support'), 2),
  score = c(80, 90, 65, 75, 85, 60, 70, 85, 80, 70),
  team = rep(c('A', 'B'), each = 5)
)
g2(df2, x = 'item', y = 'score', color = 'team') |>
  mark_area(style = list(fillOpacity = 0.5)) |>
  mark_line(style = list(lineWidth = 2)) |>
  coord_polar() |>
  scale_x(padding = 0.5, align = 0) |>
  scale_y(domainMin = 0, domainMax = 100) |>
  axis_x(grid = TRUE)
}
