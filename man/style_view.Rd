% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{style_view}
\alias{style_view}
\title{Style Plot Region Backgrounds}
\usage{
style_view(
  chart = NULL,
  margin = NULL,
  padding = NULL,
  plot = NULL,
  inset = NULL,
  content = NULL
)
}
\arguments{
\item{chart}{A \code{g2} object.}

\item{margin}{CSS background color for the margin area (outside the chart).}

\item{padding}{Background fill for the padding area (between chart edge and
plot area, where axis labels live).}

\item{plot}{Background fill for the plot area (where data is rendered).}

\item{inset}{Background fill for the main area inside the inset spacing.}

\item{content}{Background fill for the innermost content area.}
}
\value{
The modified \code{g2} object.
}
\description{
Set background fill colors for the different spatial regions of a G2 chart.
This is especially useful for visualizing how \code{margin}, \code{padding}, and
\code{inset} affect the layout (see \code{\link[=canvas]{canvas()}}).
}
\details{
The chart area is organized as nested layers:

\describe{
\item{\code{margin}}{Outermost area — space outside the chart container. Set as
a CSS \code{background-color} on the container \verb{<div>}.}
\item{\code{padding}}{Inside the container but outside the plot area — where
axis tick labels are drawn. Set via \code{theme.view.viewFill}.}
\item{\code{plot}}{The plot area where data marks are rendered. Set via
\code{theme.view.plotFill}.}
\item{\code{inset}}{Inside the plot area, surrounding the innermost data region.
Set via \code{theme.view.mainFill}.}
\item{\code{content}}{The innermost data region (inside \code{inset}). Set via
\code{theme.view.contentFill}.}
}
}
\examples{
# Visualize the three layout regions with distinct colors
g2(mtcars, hp ~ mpg) |>
  canvas(margin = 20, padding = 60, inset = 20) |>
  style_view(margin = '#fff3bf', padding = '#ffc9c9', plot = '#a5d8ff')
}
