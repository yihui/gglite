% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gglite.R
\name{canvas}
\alias{canvas}
\title{Configure Canvas Options}
\usage{
canvas(
  chart = NULL,
  width = NULL,
  height = 480,
  padding = NULL,
  margin = NULL,
  inset = NULL,
  renderer = NULL,
  ...
)
}
\arguments{
\item{chart}{A \code{g2} object, or \code{NULL} to create a deferred modifier.}

\item{width}{Width of the chart in pixels. \code{NULL} (default) enables
auto-fit to the container width.}

\item{height}{Height of the chart in pixels. Default is \code{480}.}

\item{padding, margin, inset}{Layout spacing in pixels. Each can be a scalar
(applied to all sides) or a length-4 vector \code{c(top, right, bottom, left)};
use \code{NA} to skip individual sides. \code{NULL} (the default) leaves the value
unset.}

\item{renderer}{The rendering backend: \code{"Canvas"} (default), \code{"SVG"}, or
\code{"WebGL"} (case-insensitive).}

\item{...}{Additional top-level chart options passed to \code{chart.options()} in
JavaScript (e.g., \code{clip = TRUE}).}
}
\value{
The modified \code{g2} object (or a \code{g2_mod} when \code{chart} is \code{NULL}).
}
\description{
Set chart dimensions, layout spacing, and renderer for a G2 chart. See
\url{https://pkg.yihui.org/gglite/examples/canvas.html} for full examples.
}
\examples{
p = g2(mtcars, hp ~ mpg)
p |> canvas(width = 600, height = 400)
p |> canvas(padding = 30)
p |> canvas(renderer = 'svg')
}
