% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{transform.g2}
\alias{transform.g2}
\alias{transform.character}
\title{Add a Data Transform to the Last Mark}
\usage{
\method{transform}{g2}(`_data`, type, ...)

\method{transform}{character}(`_data`, ...)
}
\arguments{
\item{_data}{A \code{g2} object (via \verb{|>}) or a transform type string (for
deferred use with \code{+}).}

\item{type}{Transform type string (e.g., \code{'stackY'}, \code{'dodgeX'}).}

\item{...}{Additional transform options (e.g., \code{thresholds}, \code{y}).}
}
\value{
The modified \code{g2} object (or a \code{g2_mod} when \verb{_data} is a string).
}
\description{
Append a data transform to the most recently added mark. G2 transforms
correspond roughly to ggplot2's \verb{stat_*()} and \verb{position_*()} functions.
}
\details{
Common transforms: \code{'stackY'} (stack, like \code{position_stack()}), \code{'dodgeX'}
(dodge, like \code{position_dodge()}), \code{'normalizeY'} (normalize to 100\%, like
\code{position_fill()}), \code{'jitterX'} / \code{'jitterY'} (jitter, like
\code{position_jitter()}), \code{'bin'} / \code{'binX'} (bin data, like \code{stat_bin()}),
\code{'groupX'} / \code{'groupY'} / \code{'groupColor'} (group and aggregate, like
\code{stat_summary()}), \code{'sortX'} / \code{'sortY'} / \code{'sortColor'} (sort data),
\code{'symmetryY'} (mirror, for pyramid / funnel charts), \code{'diffY'} (difference,
for waterfall charts), \code{'select'} / \code{'selectX'} / \code{'selectY'} (filter),
\code{'sample'} (down-sample), \code{'pack'} (circle-packing layout), \code{'flexX'}
(flexible x spacing, Marimekko charts).

\strong{Caution when using \code{+}:} S3 dispatch requires that the first argument is
unnamed. Use \code{p + transform('stackY')} (not
\code{p + transform(type = 'stackY')}). See \code{?transform.g2} for the full API.
}
\examples{
# Stacked bar chart
df = data.frame(
  x = rep(c('A', 'B'), each = 2), y = c(3, 2, 5, 4),
  color = rep(c('a', 'b'), 2)
)
g2(df, y ~ x, color = ~ color) |>
  mark_interval() |>
  transform('stackY')

# Grouped (dodged) bar chart
g2(df, y ~ x, color = ~ color) |>
  mark_interval() |>
  transform('dodgeX')

# Percent stacked bar (normalizeY + stackY)
g2(df, y ~ x, color = ~ color) |>
  mark_interval() |>
  transform('stackY') |>
  transform('normalizeY')

# Jitter on a scatter plot
g2(mtcars, hp ~ cyl) |>
  transform('jitterX')

# Histogram using binX
g2(mtcars, ~ mpg) |>
  mark_interval(encode = list(y = 'count')) |>
  transform('binX', thresholds = 15)

# With + operator (first argument must be unnamed)
g2(df, y ~ x, color = ~ color) |> mark_interval() + transform('stackY')
}
