% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/component.R
\name{labels.g2}
\alias{labels.g2}
\title{Add Labels to the Last Mark}
\usage{
\method{labels}{g2}(object = NULL, ...)
}
\arguments{
\item{object}{A \code{g2} object.}

\item{...}{Label options such as \code{text} (channel name as \code{~col} or
\code{'col'}), \code{position}, \code{formatter}, \code{style}.}
}
\value{
The modified \code{g2} object.
}
\description{
Append a label configuration to the most recently added mark. Can be called
multiple times to add several label layers. This is an S3 method on
\code{\link[base:labels]{base::labels()}}, so \code{chart |> labels(...)} dispatches here automatically.
For the \code{+} deferred-modifier syntax use \code{labels.g2(...)} directly, e.g.
\code{chart + labels.g2(text = ~ y)}.
}
\examples{
df = data.frame(x = c('A', 'B', 'C'), y = c(3, 7, 2))
p = g2(df, y ~ x)
p |> labels(text = ~ y)
p |> labels(text = ~ y, position = 'inside')
}
