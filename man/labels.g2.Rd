% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/component.R
\name{labels.g2}
\alias{labels.g2}
\alias{labels.character}
\alias{labels.formula}
\title{Add Labels to the Last Mark}
\usage{
\method{labels}{g2}(object, text, ...)

\method{labels}{character}(object, ...)

\method{labels}{formula}(object, ...)
}
\arguments{
\item{object}{A \code{g2} object, a formula \code{~col}, or a character string (the
text channel) for deferred use with \code{+}.}

\item{text}{Label text channel: a column name as \code{~col} or \code{'col'}.}

\item{...}{Additional label options such as \code{position}, \code{formatter},
\code{style}, or \code{innerHTML}.}
}
\value{
The modified \code{g2} object (or a \code{g2_mod} when \code{object} is not a
\code{g2}).
}
\description{
Append a label configuration to the most recently added mark. Can be called
multiple times to add several label layers.
}
\details{
Use \code{text} (a column name as a formula \code{~col} or a string \code{'col'}) to map
a data column to label text. G2 also supports \code{innerHTML}, which overrides
\code{text} when provided.

\strong{Caution when using \code{+}:} S3 dispatch requires that the first argument is
unnamed. Use \code{p + labels(~col)} or \code{p + labels('col')} (not
\code{p + labels(text = 'col')}). To use \code{innerHTML}, pass a dummy first
argument: \code{p + labels('', innerHTML = ~col)}. See \code{?labels.g2} for the full
API.
}
\examples{
df = data.frame(x = c('A', 'B', 'C'), y = c(3, 7, 2))
g2(df, y ~ x) |>
  labels(text = ~ y, position = 'inside')

# With + operator (first argument must be unnamed)
g2(df, y ~ x) + labels(~ y)
g2(df, y ~ x) + labels('y', position = 'inside')
}
